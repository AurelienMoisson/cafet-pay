function getBalance(studentId) {
  return -1.20
}

function getTransactions(studentId) {
  return [{
    time:"03-09-2019T13:02:47",
    regularization:10,
    products:{id
}


function getNumberOfTransactionsSinceNegative(studentId) {
  // should be 0 if account balance is positive
  // should be 1 if the student has done no other transaction
  // than the one that made it's balance negative
  return 0
}

function getTimeSinceNegative(studentId) {
  // returns the time in days since the account balance is negative
  // should be 0 if the account balance is positive
  return 0
}

function getTimeSinceLastTransaction(studentId) {
  // Time in days since last transaction
  // 0 if last transaction was today
  return 0
}
